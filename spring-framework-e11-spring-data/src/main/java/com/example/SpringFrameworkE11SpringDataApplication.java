package com.example;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class SpringFrameworkE11SpringDataApplication {

	public static void main(String[] args) {
		SpringApplication.run(SpringFrameworkE11SpringDataApplication.class, args);
	}
}

/**
 * spring data - is high leve1 layer you can use it above multiple implementations
  like sql such as jpa/Hibernate or no sql such as mongodb
 *
 */
